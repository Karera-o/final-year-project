{
  "builds": [{
    "src": "core/wsgi.py",
    "use": "@vercel/python",
    "config": {
      "runtime": "python3.7",
      "config": { "maxLambdaSize": "15mb" }
    }
  }],
  "routes": [{
    "src": "/(.*)",
    "dest": "core/wsgi.py"
  }]
}